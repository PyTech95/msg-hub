# CPaaS Hub — Product Requirements Document

## Original Problem Statement
Build a production-ready full-stack CPaaS-style web application that lets a business send SMS, WhatsApp, RCS messages and place voice calls from one unified dashboard. Multi-user with roles (Super Admin, Admin, Agent). Contacts, lists, templates, campaigns, conversation timeline, voice calls, analytics, providers, webhooks, billing/usage. Provider abstraction with mock implementations so app runs end-to-end without live keys.

## Architecture
- **Frontend:** React 19 + react-router 7 + Tailwind + shadcn/ui + Recharts (Vite-style CRA via craco)
- **Backend:** FastAPI + Motor (MongoDB async) + PyJWT + bcrypt
- **Database:** MongoDB (collections listed below)
- **Auth:** JWT email/password (Bearer header + httpOnly cookie)
- **Providers:** Async mock adapters (SMS/WhatsApp/RCS/Voice) with provider-interface; swap to live (Twilio/Gupshup/Exotel/RBM) later.

## User Personas
- **Super Admin** — full control, billing, providers, team.
- **Admin** — campaigns, templates, contacts.
- **Agent** — conversations, click-to-call, send messages.

## Core Requirements (static)
1. Contact CRUD + CSV import + lists/tags + DND flag
2. Templates per channel
3. Campaign builder + scheduler + recipient stats
4. Multi-channel send (SMS / WhatsApp / RCS / Voice)
5. Conversation timeline per contact (omnichannel)
6. Provider settings + webhook event monitor
7. Dashboard KPIs + analytics charts
8. Role-based access
9. Usage/billing records

## MongoDB Collections
users, contacts, contact_lists, templates, campaigns, campaign_recipients, messages, message_events, conversations, call_logs, provider_accounts, webhook_events, usage_records, audit_logs

## Implemented (Feb 2026)
- **Rebrand → tezsandesh.digital** (July 2 2026): logo image + orange wordmark across sidebar/login/reset/forgot pages, PDF invoice header, TOTP issuer, backend API title; theme colors shifted from blue to orange (--primary: 24 95% 53%)
- JWT auth + role guard + 2FA TOTP + brute-force lockout (5/15min) + change-password + token versioning + password reset flow (60s rate-limit)
- All 17 MongoDB collections w/ indexes
- Full REST API under /api (split between server.py + features.py + adapters/airtel_iq.py)
- Mock provider adapters: SMS / WhatsApp / RCS / Voice / **Email** with simulated async delivery
- **Airtel IQ adapter** (July 2 2026, `/app/backend/adapters/airtel_iq.py`) — SMS + WhatsApp + Voice with OAuth2 client-credentials + token cache, DLT-compliant SMS payload (customerId + dltHeaderId + dltTemplateId + templateVariables), transparent mock fallback when AIRTEL_IQ_LIVE≠1 or any credential missing. HMAC-SHA256 signature verify helper. 3 webhook endpoints wired: `/api/webhooks/airtel/{sms/dlr, whatsapp/inbound, voice/status}` — inbound WhatsApp auto-creates contact with `wa-inbound` tag. Provider Vault seeded with 3 "Airtel IQ" cards.
- Background campaign scheduler (every 30s, try/except + audit on failure)
- Audit Logs + Provider Credentials Vault + Channel Markup + Monthly Invoices (JSON + CSV + **PDF**) + CSV exports
- **AI Bill Splitter** — upload multi-bill PDF, Claude Sonnet 4.5 extracts each bill (name/phone/email/property_id/address/amount/due_date), bulk-send via SMS/WhatsApp/Email with template variables
- **Notice Templates** — HTML template + variables → WeasyPrint PDF → bulk send via Email/WhatsApp with cover message; downloadable PDFs
- **AI Voice Campaigns** — script-based outbound calls with `{{name}}` `{{amount}}` `{{property_id}}` variables, voice selection, audience picker (bills or contacts), live progress
- **Smart Reminder Automation (Feb 21, 2026)** — auto-escalating multi-channel reminders for unpaid bills: T-7 days → SMS, T-3 days → WhatsApp, T-1 day → AI Voice. Background loop every 60s. Endpoints: `POST /api/bills/enable-reminders` (min 1 bill), `POST /api/bills/{id}/mark-paid` (404 on missing bill, auto-cancels pending schedules), `GET /api/bills/{id}/schedules`, `GET /api/reminders/upcoming`. New `/reminders` page (super_admin + admin) with cadence preview + per-bill schedule view + inline Mark Paid.
- **Invoice PDF Export (Feb 21, 2026)** — `GET /api/export/invoice/{YYYY-MM}.pdf` (WeasyPrint, super_admin/admin only). PDF button added to /invoices alongside CSV + JSON.
- Pages: Dashboard (role-aware), Contacts (+ profile timeline + edit + CSV import/export + bulk delete), Lists CRUD, Templates (+ edit), Campaigns (+ wizard + detail), **Bills (AI)** (+ Auto-Remind & Mark Paid actions + Status column), **Notices**, **Voice AI**, **Reminders**, Conversations, Message Logs (+ export), Calls, Reports, Providers (+ credentials manager + 3 Airtel IQ cards), Webhooks, Audit Logs, Invoices (+ PDF), Team, Settings (+ 2FA + Markup + change-password), ForgotPassword + ResetPassword
- Role-aware sidebar + RoleRoute guards; Agent dashboard
- **VPS Production Readiness (Jun 2026)** — `/app/DEPLOYMENT.md` (Ubuntu+Nginx+SSL+systemd guide for Hostinger VPS), `.env.production.example` for backend+frontend. Changes: api.js relative `/api` fallback when REACT_APP_BACKEND_URL empty; logo self-hosted at `/logo.png` (no emergent asset URLs); `DEMO_MODE=false` → no sample seed data + WhatsApp mock sends become hard errors (`_mock_or_fail`); fixed Gunicorn boot crash (features.py reminder_loop now scheduled via router startup event, not import-time create_task); gunicorn added to requirements.txt (pip freeze); CORS via CORS_ORIGINS env. Verified: gunicorn 2-workers boot + webhook verification on :8002, `yarn build` clean (no hardcoded URLs), preview still green.
- **Meta WhatsApp Cloud API (Jun 2026)** — production integration in `adapters/meta_whatsapp.py` (Graph v22.0). Endpoints: `GET/POST /api/webhook/whatsapp` (hub.challenge verification + inbound msgs/statuses with idempotency + STOP opt-out + read→delivered mapping), `POST /api/whatsapp/send-message` (auto-creates contact, live via Meta / mock fallback), `GET /api/whatsapp/setup` (callback URL + verify token info). Credentials: Vault (provider_key `meta_whatsapp`, mock OFF) → env (`WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_VERIFY_TOKEN=tezsandesh_verify_7f3a9c1e`, optional `WHATSAPP_APP_SECRET` for X-Hub-Signature-256, `GRAPH_API_VERSION`). Providers page has Meta setup card (copy callback URL/verify token + quick test send). Live sends skip mock lifecycle simulation (real statuses via webhook); provider Test button does real Graph handshake when creds present. Self-tested via curl (verification 200/403, inbound save, status mapping, idempotency) + UI screenshot.
- **Multi-Tenant SaaS Data Isolation (Feb 12, 2026)** — completed strict company-level scoping across all CRUD routes. Fixed: `list_contacts`/`create_contact`/`get_contact`/`update_contact`/`delete_contact`/`bulk_delete_contacts` now all use `cflt(user)` + auto-inject `company_id`; `import_contacts_csv` fixed missing `user` binding (was crashing on `user.get()`); `initiate_call`/`list_calls` now scoped + persist `company_id` on `call_logs` and `usage_records`; `resolve_audience` signature updated to accept `user` (was broken — callers passed user, definition expected list); campaign scheduler updated; `campaign_recipients` scoped in detail + delete; `message_events` verifies message ownership; `notice_pdfs`/download scoped; `voice_campaign` detail's call_logs scoped; features.py `enable_reminders` + `mark_bill_paid` scoped by company. Providers endpoints (`list/create/delete/get_credentials/set_credentials/test`) now `platform_only` — Company Admin gets 403 on all global-provider routes. `emit_inbound_reply` propagates `company_id` onto conversation upserts. `audit_logs` now record `company_id`. Verified via testing agent v3 (57/57 backend tests PASS).
- **Per-tenant WhatsApp Cloud API (Feb 12, 2026)** — each Company Admin can now connect their OWN Meta WhatsApp Business Account. New collection `company_whatsapp_configs` (unique index on `company_id`) storing per-tenant `access_token`/`phone_number_id`/`waba_id`/`app_secret`/`graph_version`/`mock`/`is_active` plus an auto-generated unique `verify_token` (`tzs_<24 urlsafe bytes>`). New endpoints: `GET/PUT/DELETE /api/whatsapp/config` (company admin) + `POST /api/whatsapp/config/test` (real Meta Graph health check with tenant creds). Per-tenant webhook: `GET/POST /api/webhook/whatsapp/{company_id}` — tenant-scoped verify token dance + X-Hub-Signature-256 enforcement using tenant's app_secret; inbound messages/statuses tagged with correct `company_id`; unknown company_id → 404. `meta_wa_credentials(company_id)` resolves in order: (1) tenant config, (2) global vault, (3) env fallback. All adapters accept `**kwargs` and Meta adapter uses `company_id` kwarg to pick the right creds. Secret masking: raw `access_token`/`app_secret` never returned by any endpoint — only `*_preview` (last 4 chars) + `*_set` boolean. Company delete cascade removes the WA config row (`company_whatsapp_configs` added to TENANT_COLLECTIONS). New frontend page `/whatsapp-settings` (RoleRoute admin+super_admin; sidebar shows for tenant admins only via new `tenantOnly` flag) with 3-step flow: (1) copy Callback URL + Verify Token to Meta dashboard, (2) save credentials + toggle Mock/Active + Test connection, (3) quick send-test. Super Admin view shows platform overview + list of configured tenants + platform templates. Removed "Made with Emergent" badge from `public/index.html` per user request.
- **WhatsApp Template Send + Silent-Drop Fix (Feb 12, 2026)** — root-caused user report "whatsapp is not going": Meta silently drops free-form text sent outside 24hr window (returns `wamid` so backend thought "sent"). Fix: (1) added optional `template_name`/`template_language`/`template_components` to `SendMessageIn` + `WhatsAppSendIn`; when set for WA, `send_message` routes to `adapter.send_template()`. (2) All provider send failures now return HTTP **400** (was 502) so Cloudflare ingress passes JSON detail through to the axios toast — user now sees the actual Meta error instead of a generic Cloudflare 502 HTML page. (3) Frontend `/contacts/{id}` WhatsApp tab: new mode toggle [Free-form | Approved template] with helpful 24hr window warning when no recent inbound; template dropdown auto-loads approved templates. (4) `/whatsapp-settings` quick-send: same toggle + dropdown.
- **WhatsApp Templates Manager UI (Feb 12, 2026)** — new endpoint `GET /api/whatsapp/templates` fetches live approved templates from Meta Graph API (`/{waba_id}/message_templates`). Enriched response: `name/language/status/category/quality_score/rejected_reason/body_preview/variable_count/components`. Strict per-tenant isolation: Company Admins must configure their OWN `waba_id` — no fallback to platform env (prevents cross-tenant leaks). Super Admin uses env `WHATSAPP_WABA_ID`. New frontend Templates catalog card (`wa-templates-card`) renders in BOTH SA and Company Admin views with auto-load, refresh, and sortable APPROVED-first table. ContactProfile + WhatsAppSettings quick-send both now use a dropdown of APPROVED templates only. Verified: 100% backend + 100% frontend via testing agent v3 (iterations 12+13).
- **Wallet + Razorpay + Template Builder (Feb 12, 2026, iter 14)** — SaaS billing foundation delivered. **Wallet**: new collections `wallets` (per-tenant balance in paise, low-balance threshold), `wallet_transactions` (credit/debit audit trail), `wallet_recharge_orders` (Razorpay lifecycle). Endpoints `GET /api/wallet` (own tenant), `GET /api/wallets` (SA overview), `POST /api/wallet/adjust` (SA manual credit/debit with reason). **Send-blocking**: `send_message` + `whatsapp_send_message` atomically debit `PRICING[channel]` paise before adapter call; insufficient balance → **HTTP 402** with "Please recharge your wallet". Auto-refund on provider send failure. SA sends bypass wallet. Added to `TENANT_COLLECTIONS` cascade. **Razorpay**: full recharge plumbing — `GET /api/wallet/recharge/config`, `POST /api/wallet/recharge/order` (creates order via python `razorpay` sdk), `POST /api/wallet/recharge/verify` (atomic status CAS on `razorpay_order_id` to prevent double-credit races). Gated by `RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET` env — returns 503 gracefully when unset. **Template Builder**: `POST /api/whatsapp/templates` submits new template to Meta (name/category/language/header/body+variables/footer/buttons); `DELETE /api/whatsapp/templates/{name}` removes. body_text has `min_length=1` guard. New adapter helpers `create_message_template()` + `delete_message_template()`. Frontend: new `/wallet` page (CA balance/pricing/txns + recharge dialog with Razorpay checkout; SA all-tenant list with per-row Adjust dialog). New `TemplateBuilderDialog` component wired into `/whatsapp-settings` for both SA and CA — creates + deletes real Meta templates end-to-end. Sidebar `Wallet` nav for all admin roles. Verified: **18/18 backend + 100% frontend** via testing agent v3.
- **WebSocket + RBAC v2 + Email + Rate-limited Broadcast (Feb 12, 2026, iter 15)** — 4 P1 features shipped. **WebSocket realtime**: in-memory `WSHub` keyed by `company_id`; endpoint `/api/ws?token=<JWT>` with reject on bad token (4401), ping/pong keepalive, tenant-scoped broadcast of `message_event` (every `emit_event`), `inbound_message` (Meta webhook), `wallet_debit` (with low_balance flag). Frontend `useRealtime` hook with exponential backoff auto-reconnect (skips reconnect on clean/auth close); wired into `AppLayout` to surface global toasts for inbound messages + low-balance alerts. **RBAC v2**: added `manager` role with hierarchy (agent=1, manager=2, admin=3, super_admin=4); `require_roles` now uses min-level check so manager satisfies agent-level endpoints; CA cannot create admin/super_admin (403). Team page dropdown updated. **Email plumbing** (`services/email_service.py`): Resend + SendGrid support, HTML templates (low-balance, campaign-complete, recharge-receipt). Env-gated via `RESEND_API_KEY` / `SENDGRID_API_KEY`. New endpoints `GET /api/email/config` + `POST /api/email/test`. `wallet_alerts` collection dedupes emails per-day. **Rate-limited Broadcast**: per-channel `asyncio.Semaphore` (WA=20, SMS=40, RCS=20, Voice=5) inside campaigns. New endpoints `POST /api/campaigns/{id}/pause|resume|cancel` (manager+ for pause/resume, admin+ for cancel). Campaigns now debit wallet per-recipient with auto-refund on failure + skip-and-continue on low balance. Atomic `find_one_and_update` guard on `wallet_adjust` prevents race. Verified: **18/18 backend + 100% frontend** via testing agent v3.
- **Wallet SA list fix (Feb 18, 2026)** — `/api/wallets` (SA overview) only returned tenants whose wallet doc had been lazily created; freshly-onboarded companies were invisible. Fix: iterate `companies` collection and auto-create wallet rows with ₹0 balance on demand. Also fixed ObjectId serialization crash by popping `_id` after `insert_one`. Verified via curl — all 3 test tenants now show up.
- **WhatsApp Media Inbox (Feb 18, 2026, iter 16)** — P0 feature shipped. **Adapter** (`adapters/meta_whatsapp.py`): 4 new functions — `upload_media()` (multipart POST → media_id), `get_media_url()` (media_id → CDN URL), `download_media_bytes()` (bearer-auth CDN GET), `send_media_message()` (image/video/audio/document/sticker). **Server**: GridFS bucket `wa_media` via `AsyncIOMotorGridFSBucket`. New endpoint `POST /api/whatsapp/send-media` (multipart to/caption/file) — enforces MAX 25 MB, 5 media kinds via MIME auto-detect (image/video/audio/document/sticker), wallet debit BEFORE GridFS upload (prevents orphans on 402), refund on Meta failure, saves `messages.media = {type, mime_type, filename, size, gridfs_id, caption, download_status}`. New endpoint `GET /api/media/{gridfs_id}` streams from GridFS with company-scope enforcement (SA sees all, tenant only own → 403 on cross-tenant). `_meta_handle_inbound` extended to handle `image|video|audio|document|sticker|voice|location` — media messages fire-and-forget download to GridFS via `_download_inbound_media`, location messages stored inline with lat/lng/name/address. **Frontend** (`ContactProfile.jsx`): new `MediaPreview` component (blob-fetch with Authorization header + object URL) renders images inline, video/audio players, docs as download links, locations as Google Maps links. Compose form now has attach button (paperclip) → hidden file input → pending panel with thumbnail + filename + size + clear button. Send button relabels to "Send media" when file pending. Verified: **13/13 backend + 100% frontend** via testing agent v3.
- **Template variables + hello_world guardrails (Feb 18, 2026)** — Fixed critical bug: `_template_variable_count` only counted BODY placeholders, missing HEADER + BUTTON URL variables → sends failed with Meta #132000 "Number of parameters does not match". New helper `_template_variables_by_component` returns `{header, body, button_urls[]}` breakdown; API response includes new `variables` field. Frontend (`ContactProfile.jsx`): dynamic variable input fields render per-component (data-testid `wa-tpl-var-header-1`, `wa-tpl-var-body-1`, `wa-tpl-var-btn0-1`); `buildTemplateComponents()` constructs correct Meta payload. `hello_world` (Meta sandbox-only, #131058) now marked "⚠ SANDBOX-ONLY" in dropdowns with red warning banner + "Create your first template →" CTA. Auto-preselects first non-hello_world template. Both ContactProfile and WhatsAppSettings quick-send updated.
- **Refresh Tokens + Meta Embedded Signup scaffolding (Feb 18, 2026)** — Access token TTL cut from 1 day → 1 hour; added 7-day refresh tokens (JWT-based, revocable via `token_version` bump). New endpoint `POST /api/auth/refresh` accepts refresh via JSON body or httpOnly cookie, returns fresh access+refresh pair. `current_user` rejects refresh tokens as API access (401 "Refresh token cannot be used for API calls"). Frontend axios interceptor auto-refreshes on 401 with single-flight guard; hard logout on refresh failure. AuthContext persists `cpaas_refresh_token`. **Meta Embedded Signup**: new endpoints `GET /api/whatsapp/embedded-signup/config` (public FB App ID + Config ID; enabled:false when env vars missing) + `POST /api/whatsapp/embedded-signup/exchange` (exchange OAuth code → long-lived business token → persist to `company_whatsapp_configs` → auto-subscribe app to WABA webhooks). Frontend WhatsAppSettings shows blue "Connect WhatsApp with Facebook" one-click card (gated by `signupCfg.enabled`); FB JS SDK loaded on-demand; captures WABA/phone_number_id via postMessage from Meta signup dialog. Blocked on user's Meta Tech Partner-verified FB App creds (`FB_APP_ID`, `FB_APP_SECRET`, `FB_CONFIG_ID`) — once set, one-click onboarding activates instantly with zero code changes.



## Backlog / Next
- **P0 (user)**: DLT Entity + Header `TZSDSH` + starter Templates via SmartPing.live (`https://smartping.live/entity/login`)
- **P0 (user)**: Airtel IQ enterprise onboarding (contact sales at `airtel.in/business/airtel-iq/` → get Customer ID / Client ID / Client Secret / SMS+WhatsApp+Voice base URLs / OAuth token URL / DLT Header + Template IDs / Webhook secret). When keys arrive, set the AIRTEL_IQ_* env vars + `AIRTEL_IQ_LIVE=1` → adapter flips to live with zero code changes.
- **P1**: Increase EMERGENT_LLM_KEY budget for end-to-end Bill Splitter demo (key budget exhausted in preview env)
- **P1**: Provider Credentials UI enhancement — add Airtel IQ specific fields (Customer ID, OAuth URL, Base URLs, DLT Header/Template ID) with test-connection button
- **P0 (user)**: Configure Callback URL + Verify Token in Meta dashboard, paste Access Token + Phone Number ID in Vault (mock OFF) → live WhatsApp. Later: permanent System User token + business verification.
- **P2**: Move notice PDFs to GridFS/object storage (currently base64 in Mongo)
- **P2**: Split server.py (~1638 lines) into modular routers (auth/audit/exports/scheduler/messages/webhooks); features.py is approaching 700 lines — split Reminders + Invoice-PDF out
- **P2**: Lifespan-managed shutdown for reminder_loop (avoid duplicate schedulers under hot-reload)
- **P2**: Pagination + date-range filters on `GET /api/reminders/upcoming`
- **P2**: Fix Recharts `width/height=-1` warning on Dashboard (min-height on ResponsiveContainer parents)
- **P2**: WebSocket inbox push, OCR for scanned PDFs (Tesseract), Jinja2 templating
